"""
Lambda expressions perform a single operation and return a 
result. They are defined using the lambda keyword, followed
by its arguments, a colon and the expression to perform
1 - Complete the lambda expression to take a word as an argument
and produce a hashtag

              lambda <argument>: <expression>
"""

lambda word: "#" + word
